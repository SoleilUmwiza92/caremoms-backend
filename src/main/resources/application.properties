##############################################
# SERVER CONFIG
##############################################
server.port=${PORT:8080}

##############################################
# DATA SOURCE ? Supabase Session Pooler (IPv4)
##############################################
spring.datasource.url=jdbc:postgresql://aws-1-eu-central-2.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.aefofwbymkvhnhtwrgzf
spring.datasource.password=kyRzbgLFUW9V1ARo

# HikariCP (connection pool)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.pool-name=HikariPool

##############################################
# JPA / HIBERNATE CONFIG
##############################################
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

##############################################
# FLYWAY ? Database migrations
##############################################
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true

##############################################
# JSON / JACKSON
##############################################
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss.SSSXXX
spring.jackson.time-zone=UTC
spring.jackson.serialization.write-dates-as-timestamps=false

##############################################
# ??? SUPABASE JWT VERIFICATION (CRITICAL)
##############################################
supabase.project-id=aefofwbymkvhnhtwrgzf

spring.security.oauth2.resourceserver.jwt.issuer-uri=https://${supabase.project-id}.supabase.co/auth/v1
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://${supabase.project-id}.supabase.co/auth/v1/keys

##############################################
# CORS
##############################################
spring.mvc.cors.enabled=true
spring.web.cors.allowed-origins=https://caremoms-frontend.vercel.app,http://localhost:5173,http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.exposed-headers=Authorization
spring.web.cors.allow-credentials=true

##############################################
# LOGGING
##############################################
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=ERROR
logging.level.com.su.caremomsbackend=DEBUG
